{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },

  "// KeyVault": "Used only when UseAzure=true; set the real URI in user-secrets.",
  "KeyVault": {
    "VaultUri": ""
  }
}
